# langchainx — direnv environment
# Issue tracking
export LANGCHAINX_GH_REPO=89jobrien/langchainx
export LANGCHAINX_LINEAR_TEAM=Jobrien
export LANGCHAINX_LINEAR_QUERY=langchainx

# Inherit secrets from parent dev/.envrc if present
source_up_if_exists
